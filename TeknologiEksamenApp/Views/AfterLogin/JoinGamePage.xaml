<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TeknologiEksamenApp.Views.AfterLogin.JoinGamePage"
             Title="JoinGamePage">
    <VerticalStackLayout
        Style="{StaticResource UsualStack}">
        <Label 
            Style="{StaticResource LabelTitle2}"
            Text="Join Game"/>
        <Entry
            x:Name="EntryJoinCode"
            Placeholder="Game Join Code"/>
        <Entry
            x:Name="EntryPassword"
            Placeholder="Game Password (Optional)"
            IsPassword="True"/>
        <Button
            x:Name="BtnJoin"
            Style="{StaticResource ButtonPrimary}"
            Text="Join"
            Clicked="BtnJoinClicked"/>
        <Button
            x:Name="BtnReturn"
            Style="{StaticResource ButtonSecondary}"
            Text="Return"
            Clicked="BtnReturnClicked"/>
    </VerticalStackLayout>
</ContentPage>